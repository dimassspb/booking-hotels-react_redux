let mongoose,bcrypt;_c00‍.x([["default",()=>_c00‍.o]]);_c00‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_c00‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);

const { Schema } = mongoose;

const userSchema = new Schema(
    {
        name: {
            type: String,
            trim: true,
            required: "Name is required",
        },
        email: {
            type: String,
            trim: true,
            required: "Email is required",
            unique: true,
        },
        password: {
            type: String,
            required: true,
            min: 6,
            max: 64,
        },
    },
    { timestamps: true },
);

userSchema.pre("save", function (next) {
    let user = this;

    if (user.isModified("password")) {
        return bcrypt.hash(user.password, 12, function (err, hash) {
            if (err) {
                _c00‍.g.console.log("BCRYPT HASH ERR ", err);
                return next(err);
            }
            user.password = hash;
            return next();
        });
    } else {
        return next();
    }
});

userSchema.methods.comparePassword = function (password, next) {
    bcrypt.compare(password, this.password, function (err, match) {
        if (err) {
            _c00‍.g.console.log("COMPARE PASSWORD ERR", err);
            return next(err, false);
        }
        // if no err, we get null
        _c00‍.g.console.log("MATCH PASSWORD", match);
        return next(null, match); // true
    });
};

_c00‍.d(mongoose.model("User", userSchema));
