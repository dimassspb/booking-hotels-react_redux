let Hotel,fs;_6a2‍.x([["create",()=>create],["hotels",()=>hotels],["image",()=>image],["sellerHotels",()=>sellerHotels],["remove",()=>remove],["show",()=>show],["refresh",()=>refresh]]);_6a2‍.w("../models/hotel",[["default",["Hotel"],function(v){Hotel=v}]]);_6a2‍.w("fs",[["default",["fs"],function(v){fs=v}]]);


       const create = async (req, res) => {
    _6a2‍.g.console.log("req fields", req.fields);
    // console.log("req files", req.files);
    let fields = req.fields;
    let files = req.files;
    let hotel = new Hotel(fields);
    hotel.postedBy = req.user._id;

    if (files.image) {
        hotel.image.data = fs.readFileSync(files.image.path);
        hotel.image.contentType = files.image.type;
    }
    hotel.save((error, result) => {
        if (error) {
            _6a2‍.g.console.log("saving hotel error", error);
            res.status(404).send("Saving is error");
        }
        res.json(result);
    });
    try {
    } catch (error) {
        _6a2‍.g.console.log(error);
        res.status(404).json({ error: error.message });
    }
};

       const hotels = async (req, res) => {
    let all = await Hotel.find({})
        .limit(20)
        .select("-image.data")
        .populate("postedBy", "_id name")
        .exec();
    res.json(all);
    // console.log(all);
};

       const image = async (req, res) => {
    let hotel = await Hotel.findById(req.params.hotelId).exec();
    if (hotel && hotel.image && hotel.image.data !== null) {
        res.set("Content-type", hotel.image.contentType);
        return res.send(hotel.image.data);
    }
};

       const sellerHotels = async (req, res) => {
    let all = await Hotel.find({ postedBy: req.user._id })
        .select("-image.data")
        .populate("postedBy", "_id name")
        .exec();
    _6a2‍.g.console.log(all);
    res.send(all);
};

       const remove = async (req, res) => {
    let removed = await Hotel.findByIdAndDelete(req.params.hotelId)
        .select("-image.data")
        .exec();
    res.json(removed);
};

       const show = async (req, res) => {
    let hotel = await Hotel.findById(req.params.hotelId)
        .select("-image.data")
        .exec();
    _6a2‍.g.console.log("Hotel", hotel);
    res.json(hotel);
};

       const refresh = async (req, res) => {
    try {
        let fields = req.fields;
        let files = req.files;
        let data = { ...fields };
        if (files.image) {
            let image = {};
            image.data = fs.readFileSync(files.image.path);
            image.contentType = files.image.type;
            data.image = image;
        }
        let updated = await Hotel.findByIdAndUpdate(req.params.hotelId, data, {
            new: true,
        }).select("-image-data");
        res.json(updated);
    } catch (error) {
        _6a2‍.g.console.log(error);
        res.status(400).send("Update faild. Try again.");
    }
};
