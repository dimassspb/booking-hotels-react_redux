let express,readdirSync,cors,mongoose,config;_974‍.w("express",[["default",["express"],function(v){express=v}]]);_974‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);_974‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_974‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_974‍.w("config",[["default",["config"],function(v){config=v}]]);




const morgan = require("morgan");
require("dotenv").config();

const app = express();

// db connection
mongoose
    .connect(config.get('DATABASE'), {
        useNewUrlParser: true,
        useFindAndModify: false,
        useUnifiedTopology: true,
        useCreateIndex: true,
    })
    .then(() => console.log("DB Connected"))
    .catch((err) => _974‍.g.console.log("DB Connection Error: ", err));

// middlewares
app.use(cors());
app.use(morgan("dev"));
app.use(express.json()); //

// route middleware
readdirSync("./routes").map((r) => app.use("/api", require(`./routes/${r}`)));

// const port = process.env.PORT || 8000;
const PORT = config.get("PORT") || 8000;

app.listen(PORT, () => console.log(`Server is running on port ${PORT}`));
